{
  "name": "cloudflare-workers",
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler dev src/index.ts",
    "publish": "wrangler publish src/index.ts --name my-worker"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^3.3.1",
    "wrangler": "^0.0.16",
    "typescript": "^4.5.5"
  }
}
